# Copy to .env and adjust as needed.
MONGO_URI=mongodb://127.0.0.1:27017/gold_manager
JWT_SECRET=replace-with-a-long-random-string
PORT=3000

# Used only by `npm run seed`
SEED_USERNAME=mani
SEED_PASSWORD=1977
